{
  "name": "thirty-four",
  "private": true,
  "engines": {
    "node": ">= 8.0.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.5.4",
    "@11ty/eleventy-plugin-rss": "^1.0.3",
    "firebase-tools": "^4.2.1",
    "fs-extra": "^7.0.0",
    "glob": "^7.1.3",
    "gray-matter": "^4.0.1",
    "markdown-it": "^8.4.2",
    "rimraf": "^2.6.2",
    "workbox-build": "^3.6.2"
  },
  "scripts": {
    "build": "npm run clean && eleventy && cp -R _posts/_data _posts/_includes assets build",
    "clean": "rimraf build",
    "serve": "firebase serve --only=hosting"
  }
}
