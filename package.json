{
  "name": "thirty-four",
  "private": true,
  "engines": {
    "node": ">= 8.0.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.7.1",
    "@11ty/eleventy-plugin-rss": "^1.0.6",
    "firebase-tools": "^6.5.0",
    "fsbin": "^1.0.7",
    "pretty-bytes": "^5.1.0",
    "workbox-cli": "^5.0.0-alpha.0"
  },
  "scripts": {
    "build": "npm run clean && eleventy && npm run copy-static && workbox injectManifest",
    "clean": "fsbin remove build",
    "copy-static": "fsbin copy assets build/assets && fsbin copy _posts/_data build/_posts/_data && fsbin copy _posts/_includes build/_posts/_includes",
    "deploy": "npm run build && firebase deploy --only=hosting",
    "serve": "firebase serve --only=hosting"
  }
}
